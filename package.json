{
  "name": "app-one",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "0.9.5",
    "socket.io": "^4.2.0"
  },
  "devDependencies": {},
  "scripts": {
    "start": "npm run build && node ./src/websocketd/websocketd.js",
    "build": "react-scripts build && mv build ./src/websocketd/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
